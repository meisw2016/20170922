app:
  eureka:
    host01: localhost
    port01: 8761
server:
  port: 24172
spring:
  application:
    name: meisw-automation
eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://${app.eureka.host01}:${app.eureka.port01}/eureka/